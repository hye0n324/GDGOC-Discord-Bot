# 💡 향후 개발 및 아이디어 메모 (Roadmap)

나중에 시간이 될 때 이어서 작업할 수 있도록 기획 아이디어 및 구현 로직을 메모해 둔 문서입니다.

---

## 📌 [기획안] 구글 시트 '설정' 탭을 통한 No-Code 봇 제어 방식

### 🎯 배경 및 목적
* 비개발자(운영진, 기획자 등)가 `.env` 파일을 건드리지 않고, 구글 스프레드시트 내에서 봇의 동작과 활성화 탭을 직접 제어할 수 있도록 개선합니다.
* 배울 필요도 없이 스프레드시트의 셀 값 변경만으로 봇의 동작 모드(`기본`, `시험기간` 등)가 즉시 바뀌게 됩니다.

---

### 📊 구글 스프레드시트 `설정` 탭 구성안

구글 시트에 **`설정`** (또는 `Control`) 탭을 새로 생성합니다.

| A열 (설정 항목) | B열 (설정 값) | C열 (비고 및 설명) |
| :--- | :--- | :--- |
| **현재 활성화 탭** | `시험기간` | 봇이 읽어올 탭 이름 (`기본`, `시험기간`, `이벤트` 등) |
| **시즌 안내 멘트** | `A+ 기원 기말고사 시즌!` | Footer 등에 들어갈 시즌별 메시지 (선택 사항) |

---

### ⚙️ 추후 구현 로직 가이드 (2단계 릴레이 조회)

`utils/fortuneData.js`의 `syncFortuneData()` 실행 시 아래와 같이 2단계로 연동을 진행합니다:

1. **1차 Fetch (`설정` 탭 읽기)**:
   - URL: `https://docs.google.com/spreadsheets/d/${sheetId}/gviz/tq?tqx=out:csv&sheet=설정&_t=${Date.now()}`
   - `B2` 셀의 값(예: `시험기간`)을 읽어와 `activeSheetName` 변수에 할당.

2. **2차 Fetch (실제 데이터 탭 읽기)**:
   - URL: `https://docs.google.com/spreadsheets/d/${sheetId}/gviz/tq?tqx=out:csv&sheet=${encodeURIComponent(activeSheetName)}&_t=${Date.now()}`
   - 추출한 `시험기간` 탭에서 운세, ITem, 미션 데이터를 읽어와 봇 메모리에 즉시 동기화.

---
*작성일: 2026-09-11*
